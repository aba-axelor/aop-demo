<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<object-views xmlns="http://axelor.com/xml/ns/object-views"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://axelor.com/xml/ns/object-views
  http://axelor.com/xml/ns/object-views/object-views_5.4.xsd">

	<kanban name="project-kanban" title="Project Tasks"
		model="com.axelor.student.db.Project" columnBy="startDate" sequenceBy="startDate">
		<field name="name" />
		<field name="progress" />
		<field name="startDate" />
		<field name="endDate" />
		<field name="contact" />
		<hilite color="danger" if="progress == 0" />
		<hilite color="success" if="progress == 100" />
		<hilite color="info" if="progress &gt;= 50" />
		<hilite color="warning" if="progress &gt; 0" />
		<template><![CDATA[
<!--   <h4>{{name}}</h4> -->
  <img ng-if="contact" src="{{$image('user', 'image')}}">
<!--   <div class="card-body">{{notes}}</div> -->
<!--   <div class="card-footer"> -->
<!--     <i class='fa fa-clock-o'></i> <span>{{startDate|date:'yyyy-MM-dd HH:mm:ss'}}</span> -->
  </div>
  ]]></template>
	</kanban>
	
	<form model="com.axelor.student.db.Project" title="Project" name="project-form">
		<panel>
			<field name="name" />
			<field name="progess" />
			<field name="startDate" />
			<field name="endDate" />
			<field name="contact" />
		</panel>
	</form>

</object-views>